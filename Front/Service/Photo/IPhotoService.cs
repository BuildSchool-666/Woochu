using Front.Models.DTOModels.Photo;
using MVCModels;

namespace Front.Service.Account_setting
{
    public interface IPhotoService
    {
        //User AddPhotoForUser(int userId, PhotoForCreation photoDto);
    }
}
